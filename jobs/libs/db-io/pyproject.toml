[project]
name = "db-io"
version = "0.1.0"
description = "Shared MySQL connection handling for PMSports data pipelines"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
